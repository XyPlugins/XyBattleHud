# AI 使用说明

本项目初版使用 AI 辅助完成，AI 的作用范围如下：

- 反编译并归纳既有 GTDamage 的配置和运行行为。
- 对照 DragonCore 字体 YAML 解释字符替换与客户端图片字形的边界。
- 设计并实现本仓库的初始 Java 代码、Gradle 构建、默认配置、测试和文档。
- 执行本地构建与测试，并在具备 Git 凭据和网络时推送仓库。

AI 没有访问或修改玩家客户端资源包内容，没有生成或替换 DragonCore PNG 资源。实际服务端部署前，维护者应在测试服确认：

1. AttributePlus 版本触发的消息文本是否命中 `triggers`。
2. 每个 `digits` 字符在客户端字体配置中是否存在。
3. 飘字的高度、时长与单次战斗实体数量是否适合服务器负载。
4. XyCore 与 AttributePlus 同时安装时 `/xybh info` 的属性来源是否符合预期。

